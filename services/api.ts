// =================================================================
// الإعداد المركزي لـ API
// =================================================================
// هذا هو الملف المركزي لتحديد عنوان الواجهة الخلفية (PHP API).

/**
 * --- هام جداً: إعدادات Vercel ---
 * 
 * تم تعديل هذا الرابط ليعمل مباشرة مع Vercel.
 * سيقوم التطبيق الآن بطلب الـ API من نفس النطاق الذي يتم استضافته عليه.
 * Vercel سيقوم بتوجيه الطلبات من /api/ إلى الدوال السحابية (Serverless Functions) التي تحتوي على كود PHP الخاص بك.
 * 
 * **ماذا عليك أن تفعل:**
 * 1. تأكد من وجود مجلد `api` في جذر مشروعك يحتوي على جميع ملفات PHP.
 * 2. قم بإعداد متغيرات البيئة (Environment Variables) الخاصة بقاعدة البيانات في إعدادات مشروعك على Vercel.
 */

export const API_BASE_URL = '/api/';
